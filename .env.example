# Public origin of the deployed site. Canonical links, Open Graph URLs, robots.txt and
# sitemap.xml all derive from it — without it they point at localhost.
VITE_SITE_URL=https://busflow.example.com
